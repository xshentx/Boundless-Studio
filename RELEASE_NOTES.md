# Boundless Studio v1.0.8

发布日期：2026-07-29

`v1.0.8` 是无界创作台（Boundless Studio）的桌面稳定性修复版本，重点修复 macOS Wails 环境下中继模型列表获取失败、图片持久化恢复与清理的并发竞态，以及提示词编辑器滚轮与画布缩放冲突问题。

## 主要更新

### macOS 中继模型原生请求

- macOS 打包应用在 `wails:` 协议环境下改由 Go 原生 HTTP 客户端获取中继服务模型列表，不再依赖 WKWebView 向本地回环代理发起该请求。
- 修复 WebKit 因自定义协议或 CORS 限制，在请求到达 Go 服务前即拒绝模型列表请求的问题。
- 支持通过 Bearer API Key 访问上游模型接口。
- 保留上游 HTTP 状态码和具体错误信息，便于定位服务端配置与鉴权问题。
- 自动去重模型 ID，并由前端统一排序展示。
- Windows 与 Vite 开发环境继续使用原有本地中继代理，不改变现有请求流程。

### 图片存储并发与清理修复

- 同一图片存储键的并发恢复请求现在共享同一个加载任务，避免重复创建对象 URL。
- 修复后完成的恢复任务撤销正在被 `<img>` 使用的 blob URL，导致图片偶发失效的问题。
- 图片清理会等待画布和资产两个持久化 Store 均完成恢复，再基于完整引用集合执行一次。
- 过期清理会保护所有仍被项目或资产数据引用的图片，包括桌面文件存储与旧版 IndexedDB 数据。
- 移除恢复过程中的重复清理入口，避免 hydration 与清理并发造成仍在使用的图片被误删。

### 提示词编辑器滚轮交互

- 提示词文本框内容仍可滚动时，滚轮优先滚动编辑器文本。
- 文本框到达顶部或底部后，滚轮继续透传给画布缩放。
- 增加嵌入式滚动区域检测与 overscroll 限制，减少滚轮事件冲突。

### 测试与质量保障

- 新增 macOS 原生模型请求、Bearer 鉴权、上游状态码和错误信息保留测试。
- 新增图片并发恢复、对象 URL 生命周期、跨 Store 引用保护和过期清理竞态测试。
- 扩展提示词编辑器滚轮透传回归测试。
- 扩展 macOS 桌面请求契约检查，确保模型列表通过原生绑定获取。

## 下载

- Windows x64 可执行文件：`BoundlessStudio.exe`
- Windows x64 压缩包：`BoundlessStudio-windows-amd64.zip`
- macOS Apple Silicon 压缩包：`BoundlessStudio-macos-arm64.zip`
- SHA-256 校验文件：`SHA256SUMS.txt`

## 升级提示

- 从 `v1.0.7` 升级前，建议备份完整的 `data/` 目录。
- macOS 包仅支持 Apple Silicon（ARM64），当前自动构建产物未进行 Apple Developer ID 签名与公证。