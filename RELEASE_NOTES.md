# Boundless Studio v1.1.0

发布日期：2026-07-30

`v1.1.0` 是无界创作台（Boundless Studio）的配置恢复与 Seedance2 故事视频工作流增强版本。本版本重点保护桌面端更新重启前后的 API、模型路由和 WebDAV 配置，并支持基于故事导演、视频工作流模板及节点参考图重新生成单个分镜的视频提示词。

## 主要更新

### 配置持久化与更新安全

- 安装桌面更新前强制刷新配置 Store，确保最新的中转 API、模型路由、软件更新与 WebDAV 配置已写入持久化存储。
- 为配置增加原生主存储、原生恢复副本和 WebView 浏览器恢复副本；主存储不可用或内容损坏时可自动恢复并异步修复。
- 配置写入采用串行队列，避免较旧的异步恢复写入覆盖用户刚完成的新修改。
- 桌面配置读取失败时保持 hydration gate 并自动重试，不再用默认配置覆盖尚未恢复的真实配置。
- 严格识别 Windows `http://wails.localhost` 与 macOS `wails:` 桌面运行环境，同时保留普通主题和用户数据原有的浏览器 fallback。

### Seedance2 故事视频提示词

- 视频占位节点新增“从分镜重新生成提示词”能力，可只重写当前分镜，不影响同一工作流中的其他视频节点。
- 单镜重写会读取关联视频工作流当前的视频提示词模板；模板为空时自动使用默认模板。
- 提示词生成会结合完整故事、当前分镜的结构化上下文、人物/场景状态、镜头与连续性信息。
- 当前视频节点的分镜图、人物图、场景图和手动参考图会合并为一次多模态请求。
- 图片传输优先使用稳定的本地存储键或后端地址，减少临时 `blob:` 与 localhost URL 对桌面端请求的影响。
- 重写成功后记录所用模型、模板和生成时间，并恢复为自动提示词状态，后续仍可手动编辑。

### 工作流约束与交互

- Seedance2 视频工作流现在必须连接一个包含可用分镜的故事导演，才能创建或刷新视频占位节点。
- 移除无故事导演时按手动数量创建空白占位节点的 fallback，避免生成缺少故事上下文的视频提示词。
- 创建与刷新入口会根据故事导演连接状态禁用，并提供明确的缺失来源或空分镜提示。
- 故事导演占位构建统一使用版本化 builder，继续继承画面比例、时长、生成模式与分镜顺序。

## 质量验证

- 新增单节点故事视频提示词重新生成契约测试。
- 扩展配置 hydration、恢复副本、桌面运行时识别和更新安装前刷新测试。
- 扩展 Seedance2 故事上下文、批量提示词改写、工作流来源约束及简化界面测试。
- 发布前执行 TypeScript 类型检查、前端测试、前端生产构建、Go 测试、`go vet` 与 Windows Wails 构建。
- Windows x64 与 macOS ARM64 安装包由同一 `v1.1.0` 标签通过 GitHub Actions 构建。

## 下载文件

- `BoundlessStudio.exe`：Windows x64 独立可执行文件。
- `BoundlessStudio-windows-amd64.zip`：Windows x64 压缩包。
- `BoundlessStudio-macos-arm64.zip`：macOS Apple Silicon（ARM64）应用包。
- `SHA256SUMS.txt`：发布文件 SHA-256 校验值。

## 升级说明

- 从 `v1.0.9` 升级前，建议备份完整的 `data/` 目录。
- Windows 可下载独立 EXE 或 ZIP；启用应用内更新的用户也可在“设置 → 软件更新”中检查并安装。
- macOS 当前仅提供 Apple Silicon（ARM64）构建，请从 GitHub Releases 手动下载更新。
- macOS 自动构建产物尚未进行 Apple Developer ID 签名与公证；若 Gatekeeper 阻止首次启动，请在 Finder 中按住 Control 点击应用并选择“打开”。
