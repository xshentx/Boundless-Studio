# Boundless Studio v1.0.5

发布日期：2026-07-28

`v1.0.5` 是无界创作台（Boundless Studio）的自动更新可靠性与画布图片恢复维护版本，重点改进 Windows 客户端原位升级的启动验证和失败回滚，并修复本地临时图片地址过期后画布及故事导演图片无法恢复的问题。

## 主要更新

### Windows 自动更新可靠性

- 从版本化程序启动更新时，自动定位并更新标准的 `BoundlessStudio.exe`，避免升级后残留多个版本化可执行文件。
- 新版程序启动后写入确认标记，更新助手只有收到启动确认才会完成升级。
- 新版程序无法启动、提前退出或启动确认超时时，自动恢复并重新启动旧版本。
- 更新前的备份文件保留在目标程序所在磁盘，降低跨磁盘移动导致的失败风险。
- 启动时清理遗留的版本化客户端文件，同时保留当前程序和标准程序入口。
- 更新助手显式传递更新目录，兼容自定义数据目录并加强更新产物路径校验。
- 增加 Windows 更新目标选择、备份、启动确认、回滚和自定义数据目录测试。

### 过期画布图片恢复

- 当画布图片的 `blob:` 临时地址已经失效时，优先从本地持久化存储恢复。
- 本地副本不可用时，可根据受信任的后端图片路径重新下载并保存为持久图片。
- 恢复成功后清除旧错误状态，并将恢复后的图片标记为保留资源。
- 本地和远程来源均不可用时显示明确错误，避免节点长期停留在无效加载状态。
- 项目切换时取消仍在进行的恢复任务，远程恢复请求增加超时和中止处理。
- 阻止目录穿越和重复编码的危险图片路径，确保恢复请求限制在图片路由内。

### 状态与并发保护

- 后台图片恢复现在与当前画布状态合并，避免覆盖恢复期间发生的节点移动、标题修改、新增节点或图片替换。
- 同步更新撤销历史和助手会话中的过期图片，防止撤销操作重新引入失效地址。
- 故事导演角色和分镜图片恢复继续保持重新生成状态的一致性。
- 新创建的画布图片默认进入持久存储，降低后续出现临时图片过期的概率。

### 测试与质量保障

- 新增过期图片本地恢复、远程恢复、失败状态、并发合并、超时取消和路径安全回归测试。
- 扩展 storage key 图片恢复、Seedance2 人脸编辑器后端来源及故事导演状态测试。
- 新增 Windows 自动更新原位替换和回滚测试。

## 下载

- Windows x64 可执行文件：`BoundlessStudio.exe`
- Windows x64 压缩包：`BoundlessStudio-windows-amd64.zip`
- macOS Apple Silicon 压缩包：`BoundlessStudio-macos-arm64.zip`
- SHA-256 校验文件：`SHA256SUMS.txt`

## 升级提示

- 从 `v1.0.4` 升级前，建议备份完整的 `data/` 目录。
- macOS 包仅支持 Apple Silicon（ARM64），当前自动构建产物未进行 Apple Developer ID 签名与公证。
