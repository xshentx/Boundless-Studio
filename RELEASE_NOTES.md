# Boundless Studio v1.0.9

发布日期：2026-07-29

`v1.0.9` 是无界创作台（Boundless Studio）的 macOS 视频请求与画布交互稳定性版本，重点修复 macOS Wails 环境下 Seedance2 视频任务提交和查询失败、持久化配置可能在恢复前被默认值覆盖，以及自动更新提示偶发缺失的问题，并优化视频工作流提示词编辑和连接点交互。

## 主要更新

### macOS 视频任务原生请求

- macOS 打包应用在 `wails:` 协议环境下，改由 Go 原生 HTTP 客户端提交和查询 Seedance2 视频任务，避免 WKWebView 在请求到达本地回环服务前因自定义协议或跨域限制而拦截请求。
- 同时支持全局中继的 `/v1/videos/generations` 路径与旧版直连 `/api/tasks` 查询路径。
- 支持 Bearer API Key，并保留上游 HTTP 状态码和 JSON 响应内容。
- 视频任务 ID 使用安全路径转义，支持包含斜杠、问号等字符的不透明任务 ID。
- 原生请求仅允许视频任务创建、单任务查询和任务列表查询，并限制响应体最大为 8 MiB。
- Windows 与 Vite 开发环境继续使用现有浏览器请求流程。

### 持久化配置恢复保护

- 应用会等待本地配置 Store 完成持久化恢复，再加载公开设置并挂载交互式画布。
- 修复异步 hydration 完成前的状态写入可能将内存默认值持久化，覆盖已保存中继地址、模型路由及 WebDAV 配置的问题。
- 持久化数据读取或合并失败时会安全回退到内存默认配置，避免应用永久停留在加载页面。
- hydration 状态仅在运行时使用，不写入持久化数据。

### 自动更新提示可靠性

- 自动更新检查改为在前端全局更新监听器注册完成后启动。
- 修复后端启动阶段检查过快，更新可用事件在 WebView 就绪前发出，导致启动后没有显示更新提示的问题。
- 已知的可用更新会在组件重新挂载后继续显示；自动检查的网络错误保持静默，可在更新设置中手动重试。

### Seedance2 工作流与提示词交互

- 视频工作流提示词模板改为固定高度并在内部滚动，避免长模板持续撑高画布节点。
- 提示词模板、视频提示词编辑器及只读预览区域的滚轮事件不再触发画布缩放，包括滚动到顶部或底部时。
- 点击编辑器外部画布可可靠结束视频提示词编辑，同时保留资源提及菜单的正常选择行为。
- 用户填写的视频提示词模板提升为每条提示词的最高优先级生成规则；模板留空时自动使用标准默认模板。
- 移除视频占位节点上重复的装饰性参考图输入点，仅保留可实际连线的统一画布连接点。

### 测试与质量保障

- 新增 macOS 原生视频任务创建、查询、旧版接口、任务 ID 转义、端点白名单及响应大小限制测试。
- 新增本地配置 hydration 与持久化覆盖保护测试。
- 新增 Seedance2 连接点、提示词编辑关闭、滚轮隔离和默认模板回归测试。
- 扩展自动更新监听时序、全局视频路由与工作流布局契约测试。

## 下载

- Windows x64 可执行文件：`BoundlessStudio.exe`
- Windows x64 压缩包：`BoundlessStudio-windows-amd64.zip`
- macOS Apple Silicon 压缩包：`BoundlessStudio-macos-arm64.zip`
- SHA-256 校验文件：`SHA256SUMS.txt`

## 升级提示

- 从 `v1.0.8` 升级前，建议备份完整的 `data/` 目录。
- macOS 包仅支持 Apple Silicon（ARM64），当前自动构建产物未进行 Apple Developer ID 签名与公证。