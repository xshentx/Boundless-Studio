# Boundless Studio v1.0.6

发布日期：2026-07-28

`v1.0.6` 是无界创作台（Boundless Studio）的画布助手模型选择与视频错误提示维护版本，重点解除画布助手的固定模型白名单限制，确保已启用中继中的文本和图片模型均可选择，并让视频生成失败原因在 Seedance2 占位节点中持续可见。

## 主要更新

### 画布助手全局中继模型

- 移除画布助手文本模型和图片模型的固定白名单。
- 文本问答模式现在可选择全部已启用中继中声明的文本模型。
- 图片生成模式现在可选择全部已启用中继中声明的图片模型。
- 优先保留用户已配置的有效模型；无有效选择时回退到当前能力模型列表中的首个模型。
- 从非默认中继选择模型后，请求会自动路由到拥有该模型的中继服务。
- 增加多中继文本及图片模型的全局选择和归属路由回归测试。

### 视频生成错误详情

- 视频任务失败时优先显示供应商返回的具体错误，不再被“生成失败”等通用短消息覆盖。
- 自动清理 `protocol_only direct adapter failed` 等内部适配器前缀，使错误内容更易读。
- 针对供应商所在国家或地区不可用的情况，显示明确的中文切换供应商或线路建议。
- 保留额度不足等具体供应商诊断信息，同时为只有通用错误的响应提供简洁回退提示。

### Seedance2 占位节点

- 横版、竖版及紧凑布局的视频占位节点均可持续显示详细生成错误。
- 错误详情仅在失败、错误或超时状态显示，避免影响正常生成流程。
- 错误区域支持悬停查看完整内容，便于定位视频供应商和线路问题。

### 测试与质量保障

- 新增画布助手全局中继模型选择与请求路由测试。
- 新增客户视频任务错误优先级、地区不可用提示及 Seedance2 错误展示测试。
- 扩展 Boundless Studio 问题修复契约测试。

## 下载

- Windows x64 可执行文件：`BoundlessStudio.exe`
- Windows x64 压缩包：`BoundlessStudio-windows-amd64.zip`
- macOS Apple Silicon 压缩包：`BoundlessStudio-macos-arm64.zip`
- SHA-256 校验文件：`SHA256SUMS.txt`

## 升级提示

- 从 `v1.0.5` 升级前，建议备份完整的 `data/` 目录。
- macOS 包仅支持 Apple Silicon（ARM64），当前自动构建产物未进行 Apple Developer ID 签名与公证。
