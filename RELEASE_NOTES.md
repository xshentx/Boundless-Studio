# Boundless Studio v1.1.1

发布日期：2026-07-30

`v1.1.1` 是无界创作台（Boundless Studio）的媒体任务恢复、视频并发与错误诊断增强版本。本版本重点降低图片和视频生成在刷新、重启或网络波动后重复提交及结果丢失的风险，同时改进 Seedance2 工作流连接、上游错误展示和桌面更新默认行为。

## 主要更新

### 图片与视频任务恢复

- 将普通视频任务的任务 ID、提供方、模型及安全路由标识保存到画布节点；重新打开画布后可继续轮询，不需要重新提交生成请求。
- 普通视频与 Seedance2 视频的提交阶段均持久化唯一 submission ID；恢复提交时复用幂等键，减少刷新或重启后重复创建上游任务。
- macOS 原生视频中继与普通中继请求均支持传递 `Idempotency-Key`。
- 图片生成会接管上游返回的真实轮询任务 ID，完成、失败和重试均校验当前任务，防止旧请求覆盖较新的节点结果。
- 为故事导演人物图、分镜图、网格图以及批量图片编辑/生成增加任务预留和释放，避免恢复流程重复接管同一任务。
- 页面卸载后停止写入旧画布快照、上传恢复结果或更新运行状态，减少切换画布时的竞态覆盖。

### 视频并发与节点状态

- 视频运行状态改为按节点独立跟踪，并使用引用计数处理同一节点的提交和轮询阶段。
- 多个普通视频或 Seedance2 视频节点可以正确保持各自的运行状态，不再由单一全局运行标记互相覆盖。
- 参考资源 hydration 成功后才锁定视频节点；初始化失败不会留下无法结束的加载状态。
- 视频完成、失败和重试均同时校验 submission ID 与 task ID，过期任务不能覆盖新一轮生成。

### Seedance2 工作流交互

- 从故事导演输出连接点打开创建菜单时，可直接新建“视频工作流”，并自动建立故事导演到工作流的连接。
- 工具栏直接创建的视频占位节点统一使用 `9:16` 初始比例，不再受当前 API 配置尺寸意外影响。
- 优化 Seedance2 人脸编辑器预览：底图与编辑图层分开渲染，避免每次图层变化都重新合成整张预览图。

### 上游错误与视频下载

- 递归提取不同供应商响应中的 `error`、`detail`、`reason`、`failure_reason`、`errors` 等字段，优先展示可操作的具体错误，而不是泛化的“生成失败”。
- 识别 `failed`、`error`、`cancelled`、`expired` 等任务失败状态，并支持 HTTP 200 中携带失败 code 的响应 envelope。
- 保留桌面原生中继返回的纯文本错误正文，不再因非 JSON 响应丢失上游诊断信息。
- 视频下载返回 JSON、无效 JSON 或非 2xx 错误时显示真实错误；仅在浏览器无响应的网络/CORS 场景下回退到原始播放 URL。
- 错误格式化保留供应商原始信息，避免将视频结果 URL 误判为错误文本。

### 软件更新

- “启动时自动检测更新”默认开启；旧版配置未包含该字段时同样保持开启。
- 用户明确关闭自动检测后仍会持久化该选择，不会在下次启动时被重置。
- 调整更新开关布局、动画和无障碍标签，使窄窗口下显示更稳定。

## 质量验证

- 新增媒体生成任务恢复、视频并发与工具栏比例、故事导演视频工作流连接菜单、上游视频错误详情测试。
- 扩展普通视频、Seedance2、图片恢复、故事导演恢复、桌面更新和 macOS 原生视频中继契约测试。
- 发布前执行 TypeScript 类型检查、前端测试、前端生产构建、Go 测试、`go vet` 与 Windows Wails 构建。
- Windows x64 与 macOS ARM64 安装包由同一 `v1.1.1` 标签通过 GitHub Actions 构建。

## 下载文件

- `BoundlessStudio.exe`：Windows x64 独立可执行文件。
- `BoundlessStudio-windows-amd64.zip`：Windows x64 压缩包。
- `BoundlessStudio-macos-arm64.zip`：macOS Apple Silicon（ARM64）应用包。
- `SHA256SUMS.txt`：发布文件 SHA-256 校验值。

## 升级说明

- 从 `v1.1.0` 升级前，建议备份完整的 `data/` 目录。
- Windows 可下载独立 EXE 或 ZIP，也可在“设置 → 软件更新”中检查并安装。
- macOS 当前仅提供 Apple Silicon（ARM64）构建，请从 GitHub Releases 手动下载更新。
- macOS 自动构建产物尚未进行 Apple Developer ID 签名与公证；若 Gatekeeper 阻止首次启动，请在 Finder 中按住 Control 点击应用并选择“打开”。
